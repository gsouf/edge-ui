import React from 'react';
import { render } from '@testing-library/react';
import CodeEditor from './CodeEditor';

test('renders editor', async () => {
  // TODO this test cannot work because it needs a mock for `document.createRange`
});
